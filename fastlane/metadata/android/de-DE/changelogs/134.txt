Interne Verbesserungen für verifizierbare Builds.
• Build-Anpassungen, damit unabhängige App-Stores Ticklr direkt aus dem öffentlichen Quellcode kompilieren und verifizieren können
• Keine Funktionsänderungen — alles funktioniert genau wie zuvor
